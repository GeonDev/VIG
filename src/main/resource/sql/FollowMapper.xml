<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="FollowMapper"> 	

	<resultMap type="follow" id="FollowSelectMap">

		<association property="followingUser"         column="user_code"    javaType="user"      select="setUser"/>		
		<association property="followedUser" 		  column="user_code" 	javaType="user"	     select="setFollower"/>
		
		<collection property="followers" 		column="user_code" 	javaType="list" 	ofType="user" 		/>
		<collection property="follwings"	 	column="user_code"  javaType="list"	  	ofType="user"		/>
		
	</resultMap>
 	
 	<select id="setUser" parameterType="String">
 		Select * FROM users
 		WHERE user_code=#{value}
 	</select>
 	<select id="setFollower" parameterType="String" >
 	
 		SELECT * FROM users
 		WHERE user_code=#{value}
 	
 	</select>
	
	<insert id="addFollow" parameterType="follow">
		INSERT INTO follow(user_code, follower_code)
		VALUES (#{followingUser.userCode}, #{followedUser.userCode})

	</insert>

	<delete id="deleteFollow" parameterType="follow">
		DELETE FROM follow
      	WHERE user_code=#{followingUser.userCode} and follower_code=#{followedUser.userCode}
	</delete>
	
	<select id="getFollow" parameterType="follow" resultType="int">
	
		SELECT COUNT(*) FROM follow
		WHERE user_code=#{followingUser.userCode} and follower_code=#{followedUser.userCode}
	
	</select>
	
	<select id="getFollowAll" parameterType="String" resultMap="FollowSelectMap">
			
			SELECT * FROM follow
			WHERE user_code=#{value}
	
	</select>
	
	<!-- 내가 팔로우 한 사람 -->
	<select id="getFollowingList" parameterType="string">
	

		SELECT  users.*
		FROM follow, users
		WHERE follow.user_code=#{keyword}
		AND follow.follower_code = users.user_code
	
	</select>
	
	<!-- 나를 팔로우 한 사람 -->
	<select id="getFollowerList" parameterType="string" resultMap="UserMapper.userSelectMap">
		
		SELECT  users.*
		FROM follow, users
		WHERE follow.follower_code=#{value}
		AND follow.user_code = users.user_code
		
	</select>


	 
</mapper>


